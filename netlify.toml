# Netlify（無料プラン）でこのサイトを公開するための設定。
# Netlify の画面でリポジトリをつなぐと、この設定が自動で読まれます。
# 手動で入力する項目はありません。

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# 管理画面（/admin）で使う Identity のリダイレクト先
[[redirects]]
  from = "/admin"
  to = "/admin/"
  status = 301
